# environments / caches
.venv*/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# secrets
.env
.env.local

# shootout artifacts: converted audio, logs, and transcripts of a private client recording
shootout/audio/
shootout/logs/
shootout/out/
shootout/golden/

# vidctx output never belongs in the repo (default location is ~/.cache/vidctx anyway)
frames/
manifest.json
contact.jpg
